# Use an official Python development image
FROM python:3.9-slim

# Set the working directory
WORKDIR /workspace

# Install any dependencies required for your development environment
RUN apt-get update && apt-get install -y git 
