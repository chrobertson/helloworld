{
    "name": "helloworld",
    "build": {
      "dockerfile": "Dockerfile"
    },
    "settings": { 
      // Set *default* container specific settings.json values on container create.
    },
    "extensions": [
        "ms-azuretools.vscode-docker",
        "github.vscode-github-actions",
        "ms-python.pylint",
        "ms-python.python"
    ],
    "forwardPorts": [
      // Any ports that you want to forward
    ],
    "postCreateCommand": "echo 'You can run things here after the container is built'",
    "remoteUser": "vscode"
  }